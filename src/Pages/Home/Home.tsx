import React from "react";

import { Helmet } from "react-helmet";

const Home: React.FC = () => {
    return (
        <div>
            <Helmet title="Home" />
        </div>
    );
};

export default Home;
